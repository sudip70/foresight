{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "frontend",
      "runtimeExecutable": "python3",
      "runtimeArgs": ["-c", "import os,http.server,functools; http.server.test(HandlerClass=functools.partial(http.server.SimpleHTTPRequestHandler, directory='frontend'), port=int(os.environ.get('PORT',8080)))"],
      "port": 8080,
      "autoPort": true
    }
  ]
}
